{
  "pgb_download": {
    "next": {
      "uploadFile": {
        "parameters": {
          "file": {
            "resolve": "{{output.buffer}}"
          },
          "filename": {
            "resolve": "{{output.filename}}"
          }
        }
      }
    }
  }
}