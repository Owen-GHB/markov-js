{
  "pgb_download": {
    "next": {
      "fileOps/saveToCorpus": {
        "parameters": {
          "buffer": {
            "resolve": "{{output.buffer}}"
          },
          "filename": {
            "resolve": "{{output.filename}}"
          }
        }
      }
    }
  }
}