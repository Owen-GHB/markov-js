{
  "name": "randomize",
  "commandType": "custom",
  "description": "Randomize the characters in a word",
  "syntax": "randomize(word)",
  "parameters": [
    {
      "name": "word",
      "type": "string",
      "required": true,
      "description": "Word to randomize the characters of"
    }
  ],
  "examples": ["randomize(\"hello\")", "randomize(\"world\")"]
}