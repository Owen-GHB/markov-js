{
  "electron": {
    "sideEffects": {
      "setState": {
        "servedUIDir": "{{input.servedui}}",
        "electronPreloadPath": "{{input.electronPreloadPath}}"
      }
    },
    "successOutput": "🖥️ Electron desktop app launched!\n   UI: {{input.servedui}}\n   Preload: {{input.electronPreloadPath}}"
  }
}