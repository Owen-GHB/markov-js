{
  "http": {
    "sideEffects": {
      "setState": {
        "servedUIDir": "{{input.servedUIDir}}",
        "serverPort": "{{input.serverPort}}",
        "apiEndpoint": "{{input.apiEndpoint}}"
      }
    }  
  }
}