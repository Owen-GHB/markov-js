{
  "generate": {
    "sideEffects": {
      "setState": {
        "userTemplateDir": "{{input.userTemplateDir}}",
        "generatedUIDir": "{{input.generatedUIDir}}"
      }
    },
    "successOutput": "🎨 UI generated successfully!\n   Output: {{input.generatedUIDir}}\n   Templates: {{input.userTemplateDir}} ? {{input.userTemplateDir}} : 'built-in'\n   Generation #{{state.uiGenerationCount}}"
  }
}