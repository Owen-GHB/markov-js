{
  "set": {
    "sideEffects": {
      "setState": {
        "{{configstate}}": {
          "fromParam": "value"
        }
      }
    },
    "successOutput": "✅ Set {{configstate}} = {{value}}{{#persist}} (persisted){{/persist}}{{^persist}} (temporary){{/persist}}"
  }
}
